<Window x:Class="Sifter.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Sifter" Width="1240" Height="820" MinWidth="1060" MinHeight="680"
        WindowStartupLocation="CenterScreen">
    <DockPanel LastChildFill="True">
        <Border DockPanel.Dock="Top" Background="{StaticResource AppPanelBrush}" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0,0,0,1" Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image Width="42" Height="42" Source="Assets/Sifter.ico" Margin="0,0,14,0"/>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Sifter" FontSize="24" FontWeight="SemiBold"/>
                    <TextBlock x:Name="BuildInfoText" Foreground="{StaticResource AppMutedTextBrush}" Margin="0,3,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="SaveSettingsButton" Content="Save"/>
                    <Button x:Name="OpenLogsButton" Content="Logs"/>
                </StackPanel>
            </Grid>
        </Border>

        <StatusBar DockPanel.Dock="Bottom" BorderBrush="{StaticResource AppBorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" Text="Ready"/>
        </StatusBar>

        <Grid Margin="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="14"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Start Here" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4"/>
                    <TextBlock Text="Pick your folders, scan, then copy or favorite the selected games." Foreground="{StaticResource AppMutedTextBrush}" Margin="0,0,0,12"/>

                    <Border Style="{StaticResource PanelBorderStyle}">
                        <StackPanel>
                            <TextBlock Text="Folders" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,10"/>

                            <TextBlock Text="Gamelists" Foreground="{StaticResource AppMutedTextBrush}"/>
                            <DockPanel Margin="0,4,0,10">
                                <Button x:Name="BrowseGamelistsButton" DockPanel.Dock="Right" Content="Browse" Margin="8,0,0,0"/>
                                <TextBox x:Name="GamelistsRootText"/>
                            </DockPanel>

                            <TextBlock Text="ROMs" Foreground="{StaticResource AppMutedTextBrush}"/>
                            <DockPanel Margin="0,4,0,10">
                                <Button x:Name="BrowseRomsButton" DockPanel.Dock="Right" Content="Browse" Margin="8,0,0,0"/>
                                <TextBox x:Name="RomsRootText"/>
                            </DockPanel>

                            <TextBlock Text="Export To" Foreground="{StaticResource AppMutedTextBrush}"/>
                            <DockPanel Margin="0,4,0,10">
                                <Button x:Name="BrowseExportButton" DockPanel.Dock="Right" Content="Browse" Margin="8,0,0,0"/>
                                <TextBox x:Name="ExportRootText"/>
                            </DockPanel>

                            <TextBlock Text="Ratings" Foreground="{StaticResource AppMutedTextBrush}"/>
                            <DockPanel Margin="0,4,0,0">
                                <Button x:Name="BrowseRatingsButton" DockPanel.Dock="Right" Content="File" Margin="8,0,0,0"/>
                                <Button x:Name="UseBundledRatingsButton" DockPanel.Dock="Right" Content="Built In" Margin="8,0,0,0"/>
                                <TextBox x:Name="RatingsFileText"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>

                    <Button x:Name="DiscoverSystemsButton" Content="Discover Systems" Style="{StaticResource PrimaryButtonStyle}" Margin="0,12,0,0"/>

                    <Expander Header="Systems" IsExpanded="False">
                        <Border Style="{StaticResource PanelBorderStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <DockPanel Margin="0,0,0,8">
                                    <Button x:Name="SelectAllSystemsButton" Content="All" DockPanel.Dock="Left"/>
                                    <Button x:Name="SelectNoSystemsButton" Content="None" DockPanel.Dock="Left"/>
                                    <TextBlock x:Name="SystemCountText" Foreground="{StaticResource AppMutedTextBrush}" VerticalAlignment="Center"/>
                                </DockPanel>
                                <ScrollViewer MaxHeight="190" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="SystemListPanel"/>
                                </ScrollViewer>
                            </StackPanel>
                        </Border>
                    </Expander>

                    <Expander Header="Filters" IsExpanded="False">
                        <Border Style="{StaticResource PanelBorderStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Preset" Foreground="{StaticResource AppMutedTextBrush}"/>
                                <ComboBox x:Name="FilterPresetCombo" SelectedIndex="0" Margin="0,4,0,10">
                                    <ComboBoxItem Content="Balanced top-rated"/>
                                    <ComboBoxItem Content="Only the best"/>
                                    <ComboBoxItem Content="Small curated batch"/>
                                    <ComboBoxItem Content="Custom"/>
                                </ComboBox>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Min score" Foreground="{StaticResource AppMutedTextBrush}"/>
                                        <TextBox x:Name="MinimumScoreText" Text="75" Margin="0,4,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Per system" Foreground="{StaticResource AppMutedTextBrush}"/>
                                        <TextBox x:Name="TopPerSystemText" Text="25" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>

                                <CheckBox x:Name="ExcludeDuplicatesCheck" Content="Skip duplicate editions" IsChecked="True" Margin="0,10,0,0"/>

                                <TextBlock Text="Hide genres" Foreground="{StaticResource AppMutedTextBrush}" Margin="0,10,0,4"/>
                                <ComboBox x:Name="GenreQuickCombo" SelectedIndex="0" Margin="0,0,0,8">
                                    <ComboBoxItem Content="Do not hide genres"/>
                                    <ComboBoxItem Content="Hide sports and racing"/>
                                    <ComboBoxItem Content="Hide sports, racing, fighting"/>
                                    <ComboBoxItem Content="Choose below"/>
                                </ComboBox>
                                <WrapPanel x:Name="GenreListPanel"/>
                            </StackPanel>
                        </Border>
                    </Expander>

                    <Expander Header="Copy Options" IsExpanded="False">
                        <Border Style="{StaticResource PanelBorderStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <CheckBox x:Name="DryRunCheck" Content="Preview first" IsChecked="True"/>
                                <CheckBox x:Name="OverwriteCheck" Content="Overwrite existing files"/>
                            </StackPanel>
                        </Border>
                    </Expander>

                    <Border Style="{StaticResource PanelBorderStyle}" Margin="0,12,0,0">
                        <StackPanel>
                            <TextBlock Text="Actions" FontWeight="SemiBold" FontSize="15" Margin="0,0,0,10"/>
                            <Button x:Name="ScanButton" Content="Scan Library" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,8"/>
                            <Button x:Name="PickTopButton" Content="Pick Top Games" Margin="0,0,0,8"/>
                            <Button x:Name="PreviewCopyButton" Content="Preview Copy" Margin="0,0,0,8"/>
                            <Button x:Name="CopyButton" Content="Copy Selected" Margin="0,0,0,8"/>
                            <Button x:Name="FavoriteButton" Content="Add Selected To Favorites" Margin="0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="150"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource PanelBorderStyle}" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="SummaryTitleText" Text="No scan yet" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock x:Name="SummaryDetailText" Text="Discover systems, scan your library, then Sifter will pre-pick a simple top-rated set." Foreground="{StaticResource AppMutedTextBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="SelectAllGamesButton" Content="Select All"/>
                            <Button x:Name="ClearGameSelectionButton" Content="Clear"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid x:Name="CandidateGrid"
                          Grid.Row="1"
                          ItemsSource="{Binding Games}"
                          IsReadOnly="False"
                          SelectionMode="Extended"
                          AlternationCount="2"
                          Margin="0,0,0,12">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Pick" Binding="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="58"/>
                        <DataGridTextColumn Header="System" Binding="{Binding SystemName}" IsReadOnly="True" Width="130"/>
                        <DataGridTextColumn Header="Game" Binding="{Binding GameName}" IsReadOnly="True" Width="*"/>
                        <DataGridTextColumn Header="Score" Binding="{Binding ScoreText}" IsReadOnly="True" Width="70"/>
                        <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" IsReadOnly="True" Width="130"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" IsReadOnly="True" Width="140"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBox x:Name="LogText"
                         Grid.Row="2"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
